// Réutilise exactement le même contenu que la page débat séance.
// Le debatUid pointe vers le même modèle Debat, seul le filtrage de la liste
// change (fait dans le layout parent).
export { default } from "../../debat/[debatUid]/page";
